@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>



<div class="row">
    <div class="col-2">
        <vc:teams></vc:teams>
    </div>
    <div class="col-10">
        <h1>Replace this with whatever the active button is</h1>
        <table class="table table-bordered table-striped">

            <thead class="font-weight-bold">
                <tr>
                    <td>First Name</td>
                    <td>Middle Initial</td>
                    <td>Last Name</td>
                    <td>Address</td>
                    <td>City</td>
                    <td>State</td>
                    <td>Zip Code</td>
                    <td>Phone Number</td>
                    <td colspan="2"><a class="btn btn-primary" asp-controller="Home" asp-action="Add">New Bowler</a></td>
                </tr>
            </thead>
            <tbody>
                @foreach (var b in Model)
                {
                    <tr>
                        <td>@b.BowlerFirstName</td>
                        <td>@b.BowlerMiddleInit</td>
                        <td>@b.BowlerLastName</td>
                        <td>@b.BowlerAddress</td>
                        <td>@b.BowlerCity</td>
                        <td>@b.BowlerState</td>
                        <td>@b.BowlerZip</td>
                        <td>@b.BowlerPhoneNumber</td>
                        @*<td>@b.Team.TeamName</td>*@

                        <td><a asp-controller="Home" asp-action="Edit" asp-route-applicationid="@b.BowlerID">Edit</a></td>
                        <td><a asp-controller="Home" asp-action="Delete" asp-route-applicationid="@b.BowlerID">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<div class="row">
    <div class="col-2">
        
    </div>
    <div class="col-10">

    </div>

</div>

